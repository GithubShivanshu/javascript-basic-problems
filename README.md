In solutions readline-sync module is used to take synchronous user input directly from the terminal.
It allows the program to pause and wait for the user to enter data—very useful for basic CLI-based JavaScript programs.
Before running the scripts, install the module using npm: npm install readline-sync
Make sure you run your JavaScript files using Node.js: node filename.js
